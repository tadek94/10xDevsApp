# Large-scale & legacy code

Grupa zmian z modułu o pracy z dużym, istniejącym (legacy) kodem — analiza projektu
`withastro/astro`, a nie greenfield 10xCards. Każdy podfolder to jedna zmiana
(`<change-id>/change.md`), tak jak w `context/changes/`, tylko wydzielona tutaj,
żeby nie mieszać dwóch projektów w jednym katalogu.

Powiązana mapa: `context/map/repo-map.md`.

> Uwaga: skille 10x zakładają płaski układ `context/changes/<change-id>/`. Dla zmian
> w tej grupie wskazuj im ścieżkę zagnieżdżoną, np.
> `/10x-research large-scale-and-legacy-code/cli-infra-data-flow`.
